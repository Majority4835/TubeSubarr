services:
  tubsubarr:
    build: .
    ports:
      - "4000:4000"
    environment:
      DATABASE_URL: file:/app/prisma/dev.db
      JELLYFIN_BASE_URL: http://jellyfin:8096
      JELLYFIN_API_KEY: change-me
    volumes:
      - ./data:/app/data
      - ./prisma:/app/prisma
